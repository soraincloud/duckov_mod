# Mod 物品 ID 清单

本清单用于统一登记本仓库各个 mod 新增的物品 ID，避免多个 mod 各自分配、ID 到处乱飞。

## 使用规则

1. 普通自定义物品继续使用 `900000+` 区间。
2. `MCPrerequisite` 直接托管的共享“MC材料”物品使用独立的 `800001+` 区间，不与 `900000+` 的功能物品 / 成品物品混用。
3. 已经发布并在代码中使用的 ID 不要随意改动，避免旧存档、商店、配方、动态物品引用失效。
4. 新物品默认占用“相似物品 / 同系列物品”附近的最小可用数字 ID。
5. 当前 MC 材料共享物品已经占用：`800001`、`800002`、`800003`，分别对应 `玻璃`、`铁锭`、`金锭`。
6. 当前 MC 系列功能物品已经占用：`900001`、`900002`、`900011`、`900012`。
7. 因为 `900003` 到 `900010` 仍然空闲，后续如果继续新增相近的 MC 系列功能物品，默认应优先按从小到大的顺序回填这些空号，而不是继续直接跳到更大的数字。
8. 每添加一个新 mod，或者已有 mod 新增一个物品时，都必须在本文件对应顺序位置补登记，写清楚 mod 名、物品名、物品 ID、源码常量位置和备注。
9. 如果是 `MCPrerequisite` 托管的共享 MC 材料物品，除登记本文件外，也要同步检查 `mods/MCPrerequisite` 中的材料物品注册、仓库 `MC材料` 分类和托管 ID 列表是否一致。

## 当前登记

| 物品 ID | Mod | 物品名 | 源码常量 | 备注 |
| --- | --- | --- | --- | --- |
| `800001` | MCPrerequisite | 玻璃 | `GlassTypeId` | 共享 MC 材料；归入仓库 `MC材料` 分类 |
| `800002` | MCPrerequisite | 铁锭 | `IronIngotTypeId` | 共享 MC 材料；`MC材料` 分类 icon 使用 `ironIngot` |
| `800003` | MCPrerequisite | 金锭 | `GoldIngotTypeId` | 共享 MC 材料 |
| `900001` | EnderPearl | 末影珍珠 | `EnderPearlTypeId` | 当前最早登记的 MC 系列自定义物品 |
| `900002` | GoldenApple | 金苹果 | `GoldenAppleTypeId` | 已支持工作台配方：`8x 金锭` + `1x 苹果`；配方依赖 `MCPrerequisite` 共享金锭 |
| `900011` | TotemOfUndying | 不死图腾 | `TotemOfUndyingTypeId` | MC 系列物品 |
| `900012` | SplashHealingPotion | 喷溅治疗药水 | `EnderPearlTypeId` | 当前支持 2 套工作台配方；常量命名沿用了旧名字，但实际物品 ID 是喷溅治疗药水 |

当前没有单独功能物品登记，或需要补充说明的 mod：

| Mod | 说明 |
| --- | --- |
| CaptainDestroyAccount | 不注册新物品；只使用游戏原版现金物品 `451` |
| MCPrerequisite | 前置 mod；当前直接托管共享 MC 材料物品 `800001`、`800002`、`800003`（玻璃、铁锭、金锭），并维护共享分类与部分配方依赖逻辑 |

## 推荐分配顺序

当前已占用的 MC 材料共享物品 ID：

`800001`、`800002`、`800003`

当前优先推荐的下一个可用 MC 材料 ID 顺序：

`800004` -> `800005` -> `800006` -> `800007` -> `800008` ...

当前已占用的 MC 系列功能物品 ID：

`900001`、`900002`、`900011`、`900012`

当前优先推荐的下一个可用 MC 系列功能物品 ID 顺序：

`900003` -> `900004` -> `900005` -> `900006` -> `900007` -> `900008` -> `900009` -> `900010` -> `900013` -> `900014` ...

也就是说，后续如果继续添加共享 MC 材料，默认从 `800004` 开始往后补；如果继续添加相似类型的 MC 系列功能物品，默认从 `900003` 开始往后补，不要继续把 ID 分散到更大的空白区间。

## 维护要求

新增 mod 或新增物品时，请按本文件中的 mod 顺序维护：

1. 如果是已有 mod 新增物品，直接在该 mod 对应行补充或扩展记录。
2. 如果是新的 mod，在合适的顺序位置插入新条目，不要只追加到文档末尾。
3. 登记时至少写清楚：mod 名、物品名、物品 ID、代码里的常量名、是否需要同步前置或共享分类。
4. 分配新 ID 前，先检查本文件，确保使用的是“相似类别中的最小未占用 ID”。
5. `MCPrerequisite` 托管的共享材料物品固定走 `800001+`，并同步检查其仓库分类 `MC材料` 是否仍然使用 `ironIngot` 作为分类图标。