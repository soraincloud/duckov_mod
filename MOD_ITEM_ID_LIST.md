# Mod 物品 ID 清单

本清单用于统一登记本仓库各个 mod 新增的物品 ID，避免多个 mod 各自分配、ID 到处乱飞。

## 使用规则

1. 所有自定义物品统一使用 `900000+` 区间。
2. 已经发布并在代码中使用的 ID 不要随意改动，避免旧存档、商店、配方、动态物品引用失效。
3. 新物品默认占用“相似物品 / 同系列物品”附近的最小可用数字 ID。
4. 目前这一批 MC 系列物品已经占用：`900001`、`900002`、`900011`、`900012`。
5. 因为 `900003` 到 `900010` 仍然空闲，后续如果继续新增相近的 MC 系列物品，默认应优先按从小到大的顺序回填这些空号，而不是继续直接跳到更大的数字。
6. 每添加一个新 mod，或者已有 mod 新增一个物品时，都必须在本文件对应顺序位置补登记，写清楚 mod 名、物品名、物品 ID、源码常量位置和备注。
7. 如果是 MC 系列共享分类物品，除登记本文件外，也要检查 `mods/MCPrerequisite` 里是否需要同步更新托管的物品 ID 列表。

## 当前登记

| 物品 ID | Mod | 物品名 | 源码常量 | 备注 |
| --- | --- | --- | --- | --- |
| `900001` | EnderPearl | 末影珍珠 | `EnderPearlTypeId` | 当前最早登记的 MC 系列自定义物品 |
| `900002` | GoldenApple | 金苹果 | `GoldenAppleTypeId` | 尚未发布，已优先回填到相近系列中的最小空闲 ID |
| `900011` | TotemOfUndying | 不死图腾 | `TotemOfUndyingTypeId` | MC 系列物品 |
| `900012` | SplashHealingPotion | 喷溅治疗药水 | `EnderPearlTypeId` | 常量命名沿用了旧名字，但实际物品 ID 是喷溅治疗药水 |

未注册新物品 ID 的 mod：

| Mod | 说明 |
| --- | --- |
| CaptainDestroyAccount | 不注册新物品；只使用游戏原版现金物品 `451` |
| MCPrerequisite | 前置 mod，不直接注册物品；当前托管的共享分类物品 ID 为 `900001`、`900011`、`900012` |

## 推荐分配顺序

当前已占用的自定义物品 ID：

`900001`、`900002`、`900011`、`900012`

当前优先推荐的下一个可用 ID 顺序：

`900003` -> `900004` -> `900005` -> `900006` -> `900007` -> `900008` -> `900009` -> `900010` -> `900013` -> `900014` ...

也就是说，后续如果继续添加相似类型的 MC 系列新物品，默认从 `900003` 开始往后补，不要继续把 ID 分散到更大的空白区间。

## 维护要求

新增 mod 或新增物品时，请按本文件中的 mod 顺序维护：

1. 如果是已有 mod 新增物品，直接在该 mod 对应行补充或扩展记录。
2. 如果是新的 mod，在合适的顺序位置插入新条目，不要只追加到文档末尾。
3. 登记时至少写清楚：mod 名、物品名、物品 ID、代码里的常量名、是否需要同步前置或共享分类。
4. 分配新 ID 前，先检查本文件，确保使用的是“相似类别中的最小未占用 ID”。