# EnderPearl 资源目录（assets）

这个目录用于存放“源码仓库里的原始素材”，例如：

- `sfx/`：音效（建议 wav/ogg 等）
- `item-icons/`：物品图标/贴图（建议 png）

说明：
- `assets/bundles/enderpearl_assets` 是已打包的二进制资源文件（AssetBundle/类似产物），不要把原始素材直接塞进那个文件里。
- 根目录现有的 `preview.png` 是游戏识别 MOD / Mods 菜单预览所需文件之一，建议保留在 MOD 根目录。
