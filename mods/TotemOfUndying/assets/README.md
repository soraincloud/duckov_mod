# TotemOfUndying 资源目录（assets）

这个目录用于存放 TotemOfUndying mod 的资源文件。

## sfx（WAV）

把音效放在这里：

- `assets/sfx/`

## bundles

`assets/bundles/models/` 用于模型相关 AssetBundle。

## item-icons

`assets/item-icons/` 用于物品图标（例如 `TotemOfUndying.png`）。
